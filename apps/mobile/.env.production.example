# Production / EAS Build — set these before running:
#   eas build --platform ios --profile preview
#
# Copy to .env or set in EAS Secrets (expo.dev → Project → Secrets).

# Backend API (Railway or your deployed API URL)
EXPO_PUBLIC_API_BASE_URL=https://your-api.up.railway.app

# Firebase (same as development)
EXPO_PUBLIC_FIREBASE_API_KEY=your-api-key
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=twin-cdd01.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=twin-cdd01
EXPO_PUBLIC_FIREBASE_APP_ID=your-app-id
